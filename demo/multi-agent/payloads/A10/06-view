[
  {
    "new_string": "  A10: { id: 'A10', role: 'Integration QA', stage: 'view', deterministic: true, verify() { return true; }, status(registry) { return { ready: Object.values(registry).every(Boolean), featureCount: Object.values(registry).filter(Boolean).length }; }, fixture: 'qa-fixture', view: '<section data-domain=\"qa\"></section>' },",
    "old_string": "  A10: { id: 'A10', role: 'Integration QA', stage: 'data', deterministic: true, verify() { return true; }, status(registry) { return { ready: Object.values(registry).every(Boolean), featureCount: Object.values(registry).filter(Boolean).length }; }, fixture: 'qa-fixture' },"
  }
]
