[
  {
    "new_string": "  A08: { id: 'A08', role: 'Checkout', stage: 'core', deterministic: true, verify() { return true; }, validateCheckout(form) { const missing = ['email', 'address', 'postal'].filter((key) => !String(form[key] || '').trim()); return { ok: missing.length === 0, missing }; } },",
    "old_string": "  A08: { id: 'A08', role: 'Checkout', stage: 'broken', deterministic: false, verify() { return false; } },"
  }
]
