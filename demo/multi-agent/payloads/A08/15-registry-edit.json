[
  {
    "new_string": "  A08: { id: 'A08', role: 'Checkout', stage: 'ready', deterministic: true, verify() { return true; }, validateCheckout(form) { const missing = ['email', 'address', 'postal'].filter((key) => !String(form[key] || '').trim()); return { ok: missing.length === 0, missing }; }, fixture: 'checkout-fixture', view: '<section data-domain=\"checkout\"></section>', focusVisible: true, keyboard: true, reducedMotion: true, owner: 'A08', schemaVersion: 1, status: 'ready' },",
    "old_string": "  A08: { id: 'A08', role: 'Checkout', stage: 'manifest', deterministic: true, verify() { return true; }, validateCheckout(form) { const missing = ['email', 'address', 'postal'].filter((key) => !String(form[key] || '').trim()); return { ok: missing.length === 0, missing }; }, fixture: 'checkout-fixture', view: '<section data-domain=\"checkout\"></section>', focusVisible: true, keyboard: true, reducedMotion: true, owner: 'A08', schemaVersion: 1 },"
  }
]
